# Fix Rewrite
Options -Multiviews

# turn on rewrites
RewriteEngine on

# set the rewrite base directory
RewriteBase /

# get all characters after the slash and pass them to the script
RewriteRule ^([A-Za-z0-9-/]+)$ /t/index.php?url=$1 [L]
